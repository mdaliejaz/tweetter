<ul>
  <li ng-repeat="tweet in tweets">
    User: {{tweet.username}} <br/>
    Tweet: {{tweet.tweetText}}
    <a href="/tweets/{{tweet.id}}/edit">Edit</a>
    <a href="" ng-click="destroy(tweet.id)">Delete</a>
  </li>
</ul>

<a href="/tweets/new">Add new tweet</a>